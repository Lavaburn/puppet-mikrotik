{
  "version": 1,
  "bugs": [
    {
      "id": "bug-001",
      "timestamp": "2026-05-04T11:39:33Z",
      "error_message": "Error: Could not find a suitable provider for mikrotik_v7_ospf_interface_template (and other mikrotik_v7_* types) when running cli-puppet-device-full (all devices), but not when running cli-puppet-device-single --target ipt1.eadc.nairobi.rcswimax.com",
      "file": "lib/puppet/provider/mikrotik_api.rb",
      "root_cause": "Puppet's feature system (Puppet::Util::Feature) caches feature block results globally after first evaluation. In a multi-device puppet device run, ros_v7 is evaluated when a v6 device (eadc1/eadc2, which have mikrotik_mpls_ldp_instance with both v6+v7 providers) is processed before ipt1. The v6 device causes ros_v7 to cache 'false'. When ipt1 (RouterOS 7) is reached, ros_v7 returns cached 'false', making all mikrotik_v7_* providers appear unsuitable.",
      "fix": "Added self.suitable? override to Puppet::Provider::Mikrotik_Api base class. It detects when NetworkDevice.current transport changes (new device being processed) and clears the feature cache entries for [:ros_v6, :ros_v7, :ros_v7_12, :ros_v7_pre12] from Puppet.features. This forces re-evaluation of version features for each new device. The last transport object_id is stored on the base class so the cache is cleared exactly once per device transition. NOTE: initial implementation used @values (wrong ivar) — the correct ivar is @results (Puppet::Util::Feature internal cache, both Puppet 6 and 7). Fixed 2026-05-06.",
      "tags": ["puppet-device", "multi-device", "feature-cache", "ros_v7", "provider-selection", "mikrotik_v7"],
      "related_bugs": [],
      "occurrences": 1,
      "last_seen": "2026-05-04T11:39:33Z"
    },
    {
      "id": "bug-002",
      "timestamp": "2026-05-07T00:00:00Z",
      "error_message": "Error: Provider mikrotik_api_v6 is not functional on this host (for mikrotik_system, mikrotik_snmp_community, mikrotik_tool_email on ipt1.eadc.nairobi.rcswimax.com which is RouterOS v7)",
      "file": "lib/puppet/provider/mikrotik_api.rb",
      "root_cause": "Bug-001 fix only cleared Puppet::Util::Feature @results cache. However, Puppet::Type also caches @defaultprovider at the class level (in Puppet::Type.defaultprovider). When a v6 device runs first, @defaultprovider is set to mikrotik_api_v6 for types like mikrotik_system that have both v6+v7 providers. This cached class-level value is returned unchanged for subsequent devices (line 1764 in type.rb: 'return @defaultprovider if @defaultprovider'). Resources without an explicit provider are then assigned the v6 default, which then fails the confine check on the v7 device.",
      "fix": "Extended self.suitable? override: after clearing the feature cache, also iterate all Puppet types via Puppet::Type.eachtype and clear @defaultprovider on any type whose cached default inherits from Puppet::Provider::Mikrotik_Api (checked with dp <= base). This forces Puppet to re-select the appropriate provider (v6 or v7) based on the fresh feature evaluation for each new device.",
      "tags": ["puppet-device", "multi-device", "defaultprovider", "ros_v6", "ros_v7", "provider-selection", "mikrotik_system", "mikrotik_snmp_community", "mikrotik_tool_email"],
      "related_bugs": ["bug-001"],
      "occurrences": 1,
      "last_seen": "2026-05-07T00:00:00Z"
    }
  ]
}
